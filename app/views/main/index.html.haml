%h1 Panel principal

.row
  .large-6.columns
    .panel
      = form_for @patient do |p|
        =p.autocomplete_field :firstname, main_autocomplete_patient_firstname_path, :id_element => "#extra_data", :update_elements => {:firstname => "#extra_data"}

    #extra_data.panel{:style => "width:400px; height:200px;"}
